# Godot 4+ specific ignores
.godot/
/android/

# .import files are NOT ignored: they carry the resource UID and must match on both machines.

*.tmp
export_presets.cfg

# Claude Code permission caches. They record absolute paths from whichever
# machine approved the command, so they must not sync between the two.
.claude/
